import React, { Component } from 'react'
import { Text, View } from 'react-native'

const ProductItem = (props) => {
    return (
            <Text>
                ProductItem
            </Text>
    )
}
